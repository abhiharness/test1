apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: ${{ variables.servicename_gcr }}
  annotations:
    run.googleapis.com/minScale: '2'
spec:
  template:
    spec:
      containers:
        - image: <image>
          ports:
            - containerPort: 8080
              name: http1
